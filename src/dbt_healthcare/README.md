# dbt project — healthcare CDC

Standalone transforms for the **healthcare-cdc-dbt** bundle.

Sources: `workspace.hc_dbt_bronze.bronze_*_cdc`  
Outputs: `hc_dbt_staging`, `hc_dbt_snapshots`, `hc_dbt_marts`
