# Declarative Automation Bundles — healthcare CDC dbt

Standalone dbt lakehouse project (not nested under the SDP lakehouse repo).

Profile: `dbc-7c3eed4c`  
Bronze schema: `workspace.hc_dbt_bronze`  
dbt target: `workspace.hc_dbt` (+ `_staging` / `_marts` / `hc_dbt_snapshots`)

Read `databricks-core` and `databricks-jobs` skills for deploy/run.
